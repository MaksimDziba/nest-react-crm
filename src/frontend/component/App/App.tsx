import React, {useState, useEffect} from 'react';

export const App = () => {
  // const [text, setText] = useState({});
  //
  // useEffect(() => {
  //   fetch('api/hello').then(res => res.json()).then(data => setText(data.text));
  // }, [])

  return (
    <div>
      {/*<h1>{props}</h1>*/}
      <span>this nest app</span>
    </div>
  )
}